[project]
name = "backend"
version = "0.1.0"
description = "LAB5 backend - shop API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.9.0",
]

[project.scripts]
backend = "main:main"
